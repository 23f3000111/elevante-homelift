import type { LandingPage } from "../types";

/**
 * Campaign and regional landing pages are composed from the site's own
 * blocks so the brand never fragments. None are published yet; the first
 * will be created in the admin, not in code.
 */
export const landingPages: LandingPage[] = [];
